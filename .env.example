# Copy this file to .env and update with your actual database credentials

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=ensabun_db
DB_PORT=3306

# Server Configuration
PORT=3001

# Instructions:
# 1. Replace 'your_password_here' with your actual MySQL password
# 2. Update DB_USER if your MySQL username is different
# 3. Change DB_NAME if you want to use a different database name
# 4. Adjust PORT if you want the server to run on a different port
